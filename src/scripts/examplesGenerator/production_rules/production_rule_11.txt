A = new C LOOP(new insert remove del) B del;
B = CALL(LOOP(new B del) A IF(C, new insert contains remove remove contains A del)) del;
C = A new contains del new LOOP(insert insert) D del;
D = CALL(new D) del B new LOOP(IF(contains, remove contains) C) contains del;