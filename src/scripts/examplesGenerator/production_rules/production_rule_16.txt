A = new contains insert contains LOOP(CALL(A B) CALL(new insert remove C) CALL(D) del del del) del;
B = C new del B A LOOP(IF(insert, A));
C = new new LOOP(new remove remove del) del del remove B LOOP(B) CALL(A D) del;
D = new insert LOOP(A B C) del C;