A = new contains insert contains LOOP(CALL(A B) CALL(new insert remove C del) CALL(D));
B = C new del B A LOOP(IF(insert, A));
C = new new LOOP(new remove remove) remove B LOOP(B) CALL(A D);
D = new insert LOOP(A B C) C;