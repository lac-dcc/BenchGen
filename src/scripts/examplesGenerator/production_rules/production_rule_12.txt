A = new new D del IF(insert, remove) contains del A IF(C,new insert contains del) new del;
B = new C LOOP(contains IF(insert, D remove)) del LOOP(C);
C = CALL(IF(C,B)) del new insert D del;
D = new A new insert del CALL(IF(LOOP(new contains D del), CALL(LOOP(A)) del)) del del;